<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.text.Text?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.finance.manager.controller.RegisterController"
            prefHeight="600.0" prefWidth="800.0"
            stylesheets="@../css/style.css">
    <center>
        <StackPane>
            <children>
                <VBox styleClass="login-pane" maxWidth="450" maxHeight="580" spacing="10">
                    <padding>
                        <Insets top="30" right="40" bottom="30" left="40"/>
                    </padding>
                    
                    <!-- Title -->
                    <HBox alignment="CENTER">
                        <VBox alignment="CENTER" spacing="10">
                            <ImageView fitHeight="60" fitWidth="60" preserveRatio="true">
                                <Image url="@../images/logo.png"/>
                            </ImageView>
                            <Label styleClass="login-title" text="Register New Account"/>
                        </VBox>
                    </HBox>
                    
                    <!-- Form -->
                    <VBox spacing="15">
                        <!-- Username input -->
                        <VBox spacing="5">
                            <Label text="Username"/>
                            <TextField fx:id="usernameField" styleClass="input-field" promptText="Please enter username"/>
                        </VBox>
                        
                        <!-- Email input -->
                        <VBox spacing="5">
                            <Label text="Email"/>
                            <TextField fx:id="emailField" styleClass="input-field" promptText="Please enter email"/>
                        </VBox>
                        
                        <!-- Phone input -->
                        <VBox spacing="5">
                            <Label text="Phone"/>
                            <TextField fx:id="phoneField" styleClass="input-field" promptText="Please enter phone number"/>
                        </VBox>
                        
                        <!-- Password input -->
                        <VBox spacing="5">
                            <Label text="Password"/>
                            <PasswordField fx:id="passwordField" styleClass="input-field" promptText="Please enter password"/>
                        </VBox>
                        
                        <!-- Confirm password input -->
                        <VBox spacing="5">
                            <Label text="Confirm Password"/>
                            <PasswordField fx:id="confirmPasswordField" styleClass="input-field" promptText="Please confirm password"/>
                        </VBox>
                        
                        <!-- Error message display -->
                        <Text fx:id="errorText" fill="RED" visible="false" managed="false"/>
                        
                        <!-- Register button -->
                        <Button fx:id="registerButton" styleClass="login-button" text="Register" onAction="#handleRegister" maxWidth="Infinity"/>
                        
                        <!-- Back to login -->
                        <HBox alignment="CENTER">
                            <Button fx:id="backToLoginButton" styleClass="link-button" text="Back to Login" onAction="#handleBackToLogin"/>
                        </HBox>
                    </VBox>
                </VBox>
            </children>
        </StackPane>
    </center>
</BorderPane> 